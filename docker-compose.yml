services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: fps
      POSTGRES_PASSWORD: fps
      POSTGRES_DB: fps
    ports: ["5432:5432"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fps"]
      interval: 5s
      timeout: 3s
      retries: 10
