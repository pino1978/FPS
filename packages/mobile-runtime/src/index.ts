import{DEFAULT_MODEL_CONFIG,MODEL_CONFIG_VERSION,MODEL_VERSION,predictFixture,type MatchContext,type VenueMetrics}from'@fps/domain';import{playerImpactScore}from'@fps/player-engine';
export type Fixture={id:string;utcDate:string;status:string;home:{id:string;name:string};away:{id:string;name:string}};export type Standing={teamId:string;played:number;points:number;goalsFor:number;goalsAgainst:number;formIndex?:number;home?:VenueMetrics;away?:VenueMetrics};export type Scorer={playerId:string;playerName:string;teamId:string;teamName:string;goals:number;assists:number;penalties:number;position?:string};export type PlayerAvailability={playerName:string;teamName:string;type?:string;reason?:string};export type Enrichment={source:'API_FOOTBALL'|'UNAVAILABLE';providerFixtureId?:string;homeStarters:string[];awayStarters:string[];homeBench:string[];awayBench:string[];injuries:PlayerAvailability[];availabilityVerified:boolean};export type OfferedOdd={bookmaker:string;market:string;selection:string;odds:number;updatedAt?:string};export type ProviderCredentials={footballDataToken:string;apiFootballKey?:string};export type PredictionRow={fixture:Fixture;expectedGoalsHome:number|null;expectedGoalsAway:number|null;markets:any[];inputs:any;availability:any};
export interface SecretStore{get(key:string):Promise<string|null>;set(key:string,value:string):Promise<void>;remove(key:string):Promise<void>}export interface LocalStore{init():Promise<void>;savePredictionRun(run:any):Promise<void>;listPredictionRuns(filters?:Record<string,unknown>):Promise<any[]>;saveRecord(kind:string,record:any):Promise<void>;listRecords(kind:string,filters?:Record<string,unknown>):Promise<any[]>}
type Circuit={failures:number;openedUntil:number};
export class FootballProvider{private circuits=new Map<string,Circuit>();constructor(private credentials:ProviderCredentials,private fd='https://api.football-data.org/v4',private af='https://v3.football.api-sports.io'){}private async request(provider:string,url:string,headers:Record<string,string>){const state=this.circuits.get(provider)||{failures:0,openedUntil:0};if(state.openedUntil>Date.now())throw new Error(`${provider} circuit open`);let last:any;for(let i=0;i<3;i++){try{const r=await fetch(url,{headers});if(r.ok){this.circuits.set(provider,{failures:0,openedUntil:0});return r.json() as Promise<any>}last=new Error(`${provider} ${r.status}`);if(r.status!==429&&r.status<500)throw last}catch(e){last=e}if(i<2)await sleep(250*Math.pow(2,i))}const failures=state.failures+1;this.circuits.set(provider,{failures,openedUntil:failures>=5?Date.now()+60000:0});throw last instanceof Error?last:new Error(`${provider} request failed`)}footballData(path:string,headers:Record<string,string>={}){if(!this.credentials.footballDataToken)throw new Error('football-data token missing');return this.request('football-data',this.fd+path,{'X-Auth-Token':this.credentials.footballDataToken,...headers})}apiFootball(path:string){if(!this.credentials.apiFootballKey)throw new Error('API-Football key missing');return this.request('api-football',this.af+path,{'x-apisports-key':this.credentials.apiFootballKey})}async fixtures(competition='SA'):Promise<Fixture[]>{const d=await this.footballData(`/competitions/${competition}/matches`);return(d.matches||[]).map((m:any)=>({id:String(m.id),utcDate:m.utcDate,status:m.status,home:{id:String(m.homeTeam.id),name:m.homeTeam.name},away:{id:String(m.awayTeam.id),name:m.awayTeam.name}}))}async standings(competition='SA'):Promise<Standing[]>{const d=await this.footballData(`/competitions/${competition}/standings`),total=table(d.standings,'TOTAL'),home=table(d.standings,'HOME'),away=table(d.standings,'AWAY');return[...total.values()].map((r:any)=>({teamId:String(r.team.id),played:r.playedGames,points:r.points,goalsFor:r.goalsFor,goalsAgainst:r.goalsAgainst,formIndex:parseForm(r.form),home:venue(home.get(String(r.team.id))),away:venue(away.get(String(r.team.id)))}))}async scorers(competition='SA',limit=80):Promise<Scorer[]>{const d=await this.footballData(`/competitions/${competition}/scorers?limit=${limit}`);return(d.scorers||[]).map((s:any)=>({playerId:String(s.player.id),playerName:s.player.name,teamId:String(s.team.id),teamName:s.team.name,goals:Number(s.goals||0),assists:Number(s.assists||0),penalties:Number(s.penalties||0),position:s.player?.position||undefined}))}matchDetails(id:string){return this.footballData(`/matches/${encodeURIComponent(id)}`,{'X-Unfold-Lineups':'true','X-Unfold-Goals':'true'})}async enrichment(utcDate:string,homeName:string,awayName:string):Promise<Enrichment>{if(!this.credentials.apiFootballKey)return unavailable();try{const d=await this.apiFootball(`/fixtures?date=${utcDate.slice(0,10)}&timezone=UTC`),target=(d.response||[]).find((x:any)=>sameTeamName(x.teams?.home?.name,homeName)&&sameTeamName(x.teams?.away?.name,awayName));if(!target)return unavailable();const id=String(target.fixture.id),[lr,ir]=await Promise.allSettled([this.apiFootball(`/fixtures/lineups?fixture=${id}`),this.apiFootball(`/injuries?fixture=${id}`)]),lineups=lr.status==='fulfilled'?(lr.value.response||[]):[],injuries=ir.status==='fulfilled'?(ir.value.response||[]):[],hl=lineups.find((x:any)=>sameTeamName(x.team?.name,homeName)),al=lineups.find((x:any)=>sameTeamName(x.team?.name,awayName));return{source:'API_FOOTBALL',providerFixtureId:id,homeStarters:names(hl?.startXI),awayStarters:names(al?.startXI),homeBench:names(hl?.substitutes),awayBench:names(al?.substitutes),injuries:injuries.map((i:any)=>({playerName:i.player?.name||'',teamName:i.team?.name||'',type:i.player?.type,reason:i.player?.reason})).filter((i:any)=>i.playerName),availabilityVerified:ir.status==='fulfilled'}}catch{return unavailable()}}async odds(providerFixtureId:string):Promise<OfferedOdd[]>{if(!this.credentials.apiFootballKey)return[];try{const d=await this.apiFootball(`/odds?fixture=${encodeURIComponent(providerFixtureId)}`),rows:OfferedOdd[]=[];for(const f of d.response||[])for(const b of f.bookmakers||[])for(const bet of b.bets||[])for(const v of bet.values||[]){const m=mapOdd(bet.name,v.value),o=Number(v.odd);if(m&&Number.isFinite(o)&&o>1)rows.push({bookmaker:b.name||String(b.id),market:m.market,selection:m.selection,odds:o,updatedAt:f.update})}return rows}catch{return[]}}async result(id:string){const d=await this.footballData(`/matches/${encodeURIComponent(id)}`,{'X-Unfold-Goals':'true'});return{id:String(d.id),status:d.status,home:d.score?.fullTime?.home??null,away:d.score?.fullTime?.away??null,scorers:(d.goals||[]).map((g:any)=>g.scorer?.name||g.player?.name).filter(Boolean),lastUpdated:d.lastUpdated}}}
export class PredictionApplicationService{constructor(private provider:FootballProvider,private store:LocalStore){}async predictions(competition='SA',persist=true){const[fixtures,standings]=await Promise.all([this.provider.fixtures(competition),this.provider.standings(competition)]),byTeam=new Map(standings.map(x=>[x.teamId,x])),candidates=fixtures.filter(f=>{const d=new Date(f.utcDate).getTime()-Date.now();return d>=-1800000&&d<=172800000}).slice(0,8);let scorers:Scorer[]=[];try{if(candidates.length)scorers=await this.provider.scorers(competition,100)}catch{}const av=new Map<string,any>();for(const f of candidates)try{av.set(f.id,availabilityProfile(await this.provider.enrichment(f.utcDate,f.home.name,f.away.name),f.home.id,f.away.id,byTeam.get(f.home.id),byTeam.get(f.away.id),scorers))}catch{av.set(f.id,unavailableProfile())}const data:PredictionRow[]=fixtures.map(f=>{const h=byTeam.get(f.home.id),a=byTeam.get(f.away.id),availability=av.get(f.id)||unavailableProfile();if(!h||!a)return{fixture:f,expectedGoalsHome:null,expectedGoalsAway:null,markets:[{market:'MODEL',selection:'NO_BET',probability:0,confidence:0,dataQuality:0,fairOdds:null,status:'NO_BET',reason:'Statistiche mancanti'}],inputs:null,availability};const context:MatchContext={homeAvailabilityLoss:availability.homeAvailabilityLoss,awayAvailabilityLoss:availability.awayAvailabilityLoss,availabilityCoverage:availability.availabilityCoverage,lineupConfirmed:availability.lineupConfirmed},bundle=predictFixture(h,a,DEFAULT_MODEL_CONFIG,context),env=qualityEnvelope(h,a,bundle.markets.filter((m:any)=>m.market==='1X2').map((m:any)=>m.probability),context);return{fixture:f,expectedGoalsHome:bundle.expectedGoalsHome,expectedGoalsAway:bundle.expectedGoalsAway,inputs:{home:h,away:a,availability},availability,markets:bundle.markets.map((m:any)=>({...m,confidence:env.confidence,dataQuality:env.dataQuality,status:env.active?'ACTIVE':'NO_BET',reason:env.active?undefined:env.reason}))}});if(persist){const now=new Date().toISOString();for(const row of data)await this.store.savePredictionRun({id:`${row.fixture.id}:${MODEL_VERSION}:${Math.floor(Date.now()/300000)}`,fixtureId:row.fixture.id,fixture:row.fixture,competition,eventAt:row.fixture.utcDate,capturedAt:now,modelVersion:MODEL_VERSION,modelConfigVersion:MODEL_CONFIG_VERSION,inputSnapshot:row.inputs,markets:row.markets})}return{source:'football-data.org+api-football-free',modelVersion:MODEL_VERSION,modelConfigVersion:MODEL_CONFIG_VERSION,data}}}
export function qualityEnvelope(home:Standing,away:Standing,oneXtwo:number[],context:MatchContext={}){const min=Math.min(home.played,away.played),sample=Math.max(0,Math.min(1,min/10)),form=home.formIndex==null||away.formIndex==null?.9:1,venueCoverage=home.home?.played&&away.away?.played?1:.85,availability=context.availabilityCoverage==null?.75:Math.max(.5,Math.min(1,context.availabilityCoverage)),lineup=context.lineupConfirmed?1:.95,dq=Math.max(0,Math.min(1,sample*form*venueCoverage*availability*lineup)),sorted=[...oneXtwo].sort((a,b)=>b-a),sep=sorted.length>1?sorted[0]-sorted[1]:0,confidence=Math.max(0,Math.min(1,.45+.35*dq+.2*Math.min(1,sep*3))),active=min>=3&&dq>=.6&&confidence>=.6;return{dataQuality:dq,confidence,active,reason:active?undefined:'Confidence/Data Quality sotto soglia'}}
function availabilityProfile(e:Enrichment,homeId:string,awayId:string,home:Standing|undefined,away:Standing|undefined,scorers:Scorer[]){if(!e.availabilityVerified)return unavailableProfile();let mapped=0,hl=0,al=0;for(const injury of e.injuries){const hs=scorers.some(s=>s.teamId===homeId&&sameTeamName(s.teamName,injury.teamName)),as=scorers.some(s=>s.teamId===awayId&&sameTeamName(s.teamName,injury.teamName)),side=hs?'home':as?'away':null,teamId=side==='home'?homeId:side==='away'?awayId:undefined;if(!teamId)continue;const s=scorers.find(x=>x.teamId===teamId&&samePerson(x.playerName,injury.playerName));if(!s)continue;const team=side==='home'?home:away,impact=playerImpactScore({goals:s.goals,assists:s.assists,penalties:s.penalties,teamGoals:Math.max(1,team?.goalsFor??1),role:s.position,starterStatus:'CONFIRMED',expectedMinutes:78,availabilityVerified:true}).score;if(side==='home')hl+=impact;else al+=impact;mapped++}const injuryCoverage=e.injuries.length?mapped/e.injuries.length:1,lineupConfirmed=e.homeStarters.length>=11&&e.awayStarters.length>=11,coverage=Math.max(.55,Math.min(1,(.75+.25*injuryCoverage)*(lineupConfirmed?1:.95)));return{source:e.source,injuries:e.injuries.length,mappedInjuries:mapped,checked:true,homeAvailabilityLoss:Math.min(1,hl),awayAvailabilityLoss:Math.min(1,al),availabilityCoverage:coverage,lineupConfirmed}}function unavailableProfile(){return{source:'UNAVAILABLE',injuries:0,mappedInjuries:0,checked:false,homeAvailabilityLoss:0,awayAvailabilityLoss:0,availabilityCoverage:.75,lineupConfirmed:false}}function unavailable():Enrichment{return{source:'UNAVAILABLE',homeStarters:[],awayStarters:[],homeBench:[],awayBench:[],injuries:[],availabilityVerified:false}}function table(items:any[],type:string){return new Map<string,any>(((items||[]).find((x:any)=>x.type===type)?.table||[]).map((r:any)=>[String(r.team.id),r]))}function venue(r:any):VenueMetrics|undefined{return r?{played:Number(r.playedGames||0),points:Number(r.points||0),goalsFor:Number(r.goalsFor||0),goalsAgainst:Number(r.goalsAgainst||0)}:undefined}function parseForm(v:string|undefined){if(!v)return undefined;const xs=v.split(',').map(x=>x.trim()).filter(Boolean);return xs.length?xs.reduce((n,x)=>n+(x==='W'?1:x==='D'?.5:0),0)/xs.length:undefined}function sleep(ms:number){return new Promise(r=>setTimeout(r,ms))}function names(xs:any[]|undefined){return(xs||[]).map(x=>x.player?.name).filter(Boolean)}function normalize(v:string|undefined){return(v||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/\b(fc|ac|ssc|ss|calcio|club)\b/g,'').replace(/[^a-z0-9]/g,'')}function sameTeamName(a:string|undefined,b:string|undefined){const x=normalize(a),y=normalize(b);return!!x&&!!y&&(x===y||x.includes(y)||y.includes(x))}function samePerson(a:string,b:string){return normalize(a)===normalize(b)}function mapOdd(name:string|undefined,value:string|undefined){const n=(name||'').toLowerCase(),v=(value||'').trim();if(n.includes('match winner')){if(/^home$/i.test(v))return{market:'1X2',selection:'1'};if(/^draw$/i.test(v))return{market:'1X2',selection:'X'};if(/^away$/i.test(v))return{market:'1X2',selection:'2'}}if(n.includes('goals over/under')&&!n.includes('half')){const m=v.match(/(Over|Under)\s*(\d+(?:\.\d+)?)/i);if(m)return{market:`OVER_UNDER_${m[2].replace('.','_')}`,selection:`${m[1].toUpperCase()} ${m[2]}`}}if(n.includes('both teams')||n.includes('both team')){if(/^yes$/i.test(v))return{market:'BTTS',selection:'GOAL'};if(/^no$/i.test(v))return{market:'BTTS',selection:'NO GOAL'}}return null}
