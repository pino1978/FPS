export type SettlementDecision='WIN'|'LOSS'|'VOID'|'UNSUPPORTED';export type SettlementContext={home:number;away:number;scorers?:string[]};
const norm=(v:string)=>v.toUpperCase().trim(),person=(v:string)=>v.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]/g,''),scorerName=(v:string)=>v.replace(/\s+(segna|to score|scores?)\s*$/i,'').trim();
export function settleMvpMarket(market:string,selection:string,ctx:SettlementContext):SettlementDecision{const s=norm(selection),total=ctx.home+ctx.away,outcome=ctx.home>ctx.away?'1':ctx.home<ctx.away?'2':'X';if(market==='1X2')return s===outcome?'WIN':'LOSS';if(market==='DOUBLE_CHANCE'){if(s==='1X')return outcome==='1'||outcome==='X'?'WIN':'LOSS';if(s==='X2')return outcome==='X'||outcome==='2'?'WIN':'LOSS';if(s==='12')return outcome==='1'||outcome==='2'?'WIN':'LOSS';return'UNSUPPORTED'}if(market==='DRAW_NO_BET'){if(outcome==='X')return'VOID';if(s.startsWith('1'))return outcome==='1'?'WIN':'LOSS';if(s.startsWith('2'))return outcome==='2'?'WIN':'LOSS';return'UNSUPPORTED'}if(market==='BTTS'){const yes=ctx.home>0&&ctx.away>0;if(s==='GOAL')return yes?'WIN':'LOSS';if(s==='NO GOAL')return yes?'LOSS':'WIN';return'UNSUPPORTED'}const ou=s.match(/(OVER|UNDER)\s*(\d+(?:\.\d+)?)/);if(ou&&market.startsWith('OVER_UNDER_')){const l=Number(ou[2]);return ou[1]==='OVER'?(total>l?'WIN':'LOSS'):(total<l?'WIN':'LOSS')}if(ou&&market.startsWith('HOME_GOALS_')){const l=Number(ou[2]);return ou[1]==='OVER'?(ctx.home>l?'WIN':'LOSS'):(ctx.home<l?'WIN':'LOSS')}if(ou&&market.startsWith('AWAY_GOALS_')){const l=Number(ou[2]);return ou[1]==='OVER'?(ctx.away>l?'WIN':'LOSS'):(ctx.away<l?'WIN':'LOSS')}if(market==='EXACT_SCORE')return s===`${ctx.home}-${ctx.away}`?'WIN':'LOSS';if(market==='MULTIGOAL'){const r=s.match(/(\d+)\s*-\s*(\d+)/);if(!r)return'UNSUPPORTED';return total>=Number(r[1])&&total<=Number(r[2])?'WIN':'LOSS'}if(market==='CLEAN_SHEET'){if(s==='HOME YES')return ctx.away===0?'WIN':'LOSS';if(s==='AWAY YES')return ctx.home===0?'WIN':'LOSS';return'UNSUPPORTED'}if(market==='WIN_TO_NIL'){if(s==='HOME YES')return ctx.home>ctx.away&&ctx.away===0?'WIN':'LOSS';if(s==='AWAY YES')return ctx.away>ctx.home&&ctx.home===0?'WIN':'LOSS';return'UNSUPPORTED'}if(market==='WIN_MARGIN'){const m=ctx.home-ctx.away;if(s==='HOME BY 1')return m===1?'WIN':'LOSS';if(s==='HOME BY 2+')return m>=2?'WIN':'LOSS';if(s==='DRAW')return m===0?'WIN':'LOSS';if(s==='AWAY BY 1')return m===-1?'WIN':'LOSS';if(s==='AWAY BY 2+')return m<=-2?'WIN':'LOSS';return'UNSUPPORTED'}if(market==='GOALS_PARITY'){if(s==='EVEN')return total%2===0?'WIN':'LOSS';if(s==='ODD')return total%2===1?'WIN':'LOSS';return'UNSUPPORTED'}if(market==='COMBO'){if(s==='1 + OVER 1.5')return outcome==='1'&&total>1.5?'WIN':'LOSS';if(s==='2 + OVER 1.5')return outcome==='2'&&total>1.5?'WIN':'LOSS';if(s==='1 + GOAL')return outcome==='1'&&ctx.home>0&&ctx.away>0?'WIN':'LOSS';if(s==='2 + GOAL')return outcome==='2'&&ctx.home>0&&ctx.away>0?'WIN':'LOSS';return'UNSUPPORTED'}if(market==='ANYTIME_SCORER'){if(!ctx.scorers)return'UNSUPPORTED';const target=person(scorerName(selection));return ctx.scorers.some(n=>person(n)===target)?'WIN':'LOSS'}return'UNSUPPORTED'}
